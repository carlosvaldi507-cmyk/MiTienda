rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // La tienda actual no usa Firestore desde el cliente. Mantener la base
    // cerrada evita que una persona externa lea o modifique datos por URL.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
